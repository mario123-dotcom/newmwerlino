{
  "name": "newmerlino",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "start": "node scripts/start.js",
    "start:reuse": "node scripts/start.js --reuse-segs",
    "start:reuse:dir": "node scripts/start.js --reuse-segs --segsDir src/temp",
    "test": "npm run build && node --test dist/**/*.test.js"
  },
  "dependencies": {
    "ffmpeg-static": "^5.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.5.0"
  }
}
